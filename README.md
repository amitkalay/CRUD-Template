# CRUD-Template

To get started, run 
``` python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt```

Then run: ``` uvicorn main:app --reload ```

You can then execute requests in the docs page at: http://127.0.0.1:8000/docs